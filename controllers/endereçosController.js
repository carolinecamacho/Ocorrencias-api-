const express = require('express');
const router = express.Router();
const bodyParser = require('body-parser');

router.get('/endereço', (req, res) => {
    const { endereco } = req.body
    
    
})


module.exports = router;